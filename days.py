import datetime
 
class days:

    def days_ago(n):
        return date.today() - timedelta(n)

    stockTime = print(str.format("starting time is: {}",days_ago(100)))

    def add_days():
        return stockTime + timedelta(10)

    add_days()


      